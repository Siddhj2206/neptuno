[daemon]
# Forward-compat: GDM >= 51 (Fedora 45+) honours FallbackSession as the
# system-wide default session for users without a saved choice.
FallbackSession=niri
# No gnome-initial-setup on a niri image: never run it for any user.
InitialSetupEnable=false

# Neptuno live ISO — single source of truth (purebuild, live-only)
#
# Sourced by Justfile and iso/src/configure-live.sh.
# Change an image ref, label or SHA here and the pipeline follows.

ISO_LABEL="NEPTUNO"
ISO_OUTPUT="output/neptuno-live.iso"
LIVE_IMAGE="localhost/neptuno-live:stable"
IMGREF="localhost/neptuno:stable"

# ── Purebuild engine ────────────────────────────────────────────────────────
# Pinned tacklebox commit that provides cmd/purebuild (pure-Go EROFS ISO).
# Update via `just bump-purebuild <new-sha>` or manual edit + rebuild.
TACKLEBOX_SHA="f3dd168bf15b235b554e497e7192a64a6563c4a4"
PUREBUILD_BIN=".build/purebuild/purebuild"
